version: "3.9"

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: regenova_chromadb
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/chroma/chroma
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 30s
      timeout: 10s
      retries: 3

  neo4j:
    image: neo4j:5
    container_name: regenova_neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    environment:
      - NEO4J_AUTH=${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-changeme}
      - NEO4J_PLUGINS=["apoc"]
      - NEO4J_dbms_security_procedures_unrestricted=apoc.*
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "cypher-shell", "-u", "${NEO4J_USER:-neo4j}", "-p", "${NEO4J_PASSWORD:-changeme}", "RETURN 1"]
      interval: 30s
      timeout: 10s
      retries: 5

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: regenova_api
    ports:
      - "8000:8000"
      # Prescriber-facing SPA is available at http://localhost:8000/app
      # API docs are available at http://localhost:8000/docs
    env_file:
      - .env
    environment:
      - CHROMA_PERSIST_DIR=/chroma/chroma
      - VECTOR_DB_BACKEND=chroma
    volumes:
      - ./data:/app/data
      - ./apps/frontend:/app/apps/frontend:ro
    depends_on:
      chromadb:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  admin:
    # Developer admin dashboard — Streamlit GUI
    # Available at http://localhost:8501
    build:
      context: .
      dockerfile: apps/admin/Dockerfile
    container_name: regenova_admin
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - API_BASE_URL=http://api:8000
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

  # NOTE: For local dev, Neo4j Browser is available at http://localhost:7474
  # ChromaDB REST API is available at http://localhost:8001/api/v1/

volumes:
  chroma_data:
  neo4j_data:
  neo4j_logs:
